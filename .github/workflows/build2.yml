name: 正式版 Build zink (aarch64)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'refs/tags/**'
  schedule:
    - cron: '0 6 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
    - name: 检出仓库
      uses: actions/checkout@v4
      
    - name: 修复依赖
      run: |
          sudo apt clean
          sudo apt autoclean
          sudo rm -rf /var/lib/apt/lists/*
          sudo apt update
          sudo rm /var/lib/apt/lists/lock
          sudo rm /var/cache/apt/archives/lock

    - name: 安装依赖
      run: |
        sudo sed -i "s/^Types: deb$/Types: deb deb-src/" /etc/apt/sources.list.d/ubuntu.sources
        sudo apt update
        sudo apt install libxrandr-dev libxxf86vm-dev libxcb-shm0-dev libxcb-glx0-dev libxext-dev libx11-dev libx11-xcb-dev libxcb1-dev libxcb-dri3-dev libxcb-present-dev libxcb-randr0-dev libxcb-sync-dev libxcb-xfixes0-dev libxshmfence-dev libdrm-dev libgbm-dev libegl1-mesa-dev libexpat1-dev libwayland-dev -y
        sudo apt install -y python3-pip
        pip3 install --user meson ninja mako

    - name: 克隆 Mesa 正式版
      run: |
          echo "📥 克隆 Mesa 仓库..."
          git clone https://gitlab.freedesktop.org/mesa/mesa.git
          cd mesa

          echo "🔍 获取最新稳定标签..."
          LATEST_TAG=$(git describe --tags `git rev-list --tags --max-count=1`)
          echo "最新标签: $LATEST_TAG"

          git checkout $LATEST_TAG
          echo "LATEST_TAG=$LATEST_TAG" >> $GITHUB_ENV

          echo "🧾 最新提交:"
          git log -1 --oneline --decorate
          MESA_COMMIT=$(git rev-parse --short HEAD)
          echo "MESA_COMMIT=$MESA_COMMIT" >> $GITHUB_ENV

    - name: 构建 Mesa (Zink only)
      run: |
        cd mesa
        sudo mkdir -p /data/data/com.termux/files/usr/glibc
        sudo chmod 777 -R /data
        mkdir -p builddir
        meson setup builddir \
          --libdir=lib \
          --prefix=/data/data/com.termux/files/usr/glibc \
          -Dbuildtype=release \
          -Dgallium-drivers=zink,freedreno \
          -Dvulkan-drivers= \
          -Dglvnd=disabled \
          -Dplatforms=x11 \
          -Degl-native-platform=x11 \
          -Dxmlconfig=disabled \
          -Dllvm=disabled \
          -Dxlib-lease=enabled \
          -Dopengl=true \
          -Dglx=dri \
          -Dglx-direct=true \
          -Dgles1=enabled \
          -Dgles2=enabled \
          -Degl=enabled
        ninja -C builddir
        ninja -C builddir install

        VERSION=$(cat VERSION)
        echo "VERSION=$VERSION" >> $GITHUB_ENV

        cd /data/data/com.termux/files/usr/glibc/lib
        rm -rf dri gbm pkgconfig

    - name: 打包 lib 文件夹
      run: |
        # 切换到 lib 目录
        cd /data/data/com.termux/files/usr/glibc/lib
        
        # 创建压缩包（仅包含 lib 目录内容）
        LIB_TAR="termux-glibc-mesa-libs-${VERSION}-aarch64.tar.gz"
        
        # 打包当前目录（lib）的所有内容
        tar -czf $GITHUB_WORKSPACE/$LIB_TAR *
        
        echo "LIB_TAR=$LIB_TAR" >> $GITHUB_ENV
        
        echo "📦 已打包 lib 目录内容:"
        ls -la /data/data/com.termux/files/usr/glibc/lib/

    - name: 生成 SHA256 校验
      run: |
        cd $GITHUB_WORKSPACE
        sha256sum $LIB_TAR > $LIB_TAR.sha256
        echo "🔐 生成校验文件完成"

    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: mesa-libs-${{ env.VERSION }}-aarch64
        path: |
          ${{ env.LIB_TAR }}
          ${{ env.LIB_TAR }}.sha256

    - name: 发布到 GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: mesa-${{ env.VERSION }}
        name: 正式版 Mesa ${{ env.VERSION }} (aarch64, Termux glibc)
        body: |
          🔧 **Mesa 自动构建信息**
          - 🧱 版本: **${{ env.VERSION }}**
          - 💡 提交: `${{ env.MESA_COMMIT }}`
          - 📍 架构: aarch64
          - 🕒 自动构建与打包成功
          - 📦 产物：
            - `${{ env.LIB_TAR }}` → 仅包含 lib 目录的 Mesa 库文件
          - 🔐 附带 `.sha256` 校验文件
          
          **使用方法：**
          1. 解压到 Termux 的 glibc 环境：`tar -xzf ${{ env.LIB_TAR }} -C $PREFIX/glibc/lib`
          2. 设置环境变量（如果需要）
        files: |
          ${{ env.LIB_TAR }}
          ${{ env.LIB_TAR }}.sha256
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 输出总结信息
      run: |
        echo "======================================"
        echo "    ✅ Mesa 构建与打包完成"
        echo "======================================"
        echo "🔖 Mesa 版本: ${{ env.VERSION }}"
        echo "💾 提交哈希: ${{ env.MESA_COMMIT }}"
        echo "📦 产物: ${{ env.LIB_TAR }}"
        echo "📁 包含: lib 目录的所有库文件"
        echo "📅 构建时间: $(date)"
        uname -a
