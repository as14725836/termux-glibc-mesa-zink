name: 正式版 Zink (aarch64)

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: '0 6 * * *'  # 🕕 每天早上 6 点自动检查 Mesa 是否有新稳定版并构建

permissions:
  contents: write  # 创建 Tag 和 Release

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
    - name: 检出仓库
      uses: actions/checkout@v4

    - name: 安装依赖
      run: |
        sudo sed -i "s/^Types: deb$/Types: deb deb-src/" /etc/apt/sources.list.d/ubuntu.sources
        sudo apt update
        sudo apt install -y libxrandr-dev libxxf86vm-dev libxcb-shm0-dev libxcb-glx0-dev \
          libxext-dev libx11-dev libx11-xcb-dev libxcb1-dev libxcb-dri3-dev libxcb-present-dev \
          libxcb-randr0-dev libxcb-sync-dev libxcb-xfixes0-dev libxshmfence-dev libdrm-dev \
          libgbm-dev libegl1-mesa-dev libexpat1-dev libwayland-dev python3-pip
        pip3 install --user meson ninja mako

    - name: 获取最新 Mesa 稳定版 Tag
      id: get_release
      run: |
        git ls-remote --tags https://gitlab.freedesktop.org/mesa/mesa.git | \
          awk -F/ '/refs\/tags\/[0-9]+\.[0-9]+\.[0-9]+$/ {print $3}' | \
          sort -V | tail -n1 > latest_mesa_tag.txt
        LATEST_TAG=$(cat latest_mesa_tag.txt)
        echo "最新稳定版 Mesa Tag: $LATEST_TAG"
        echo "MESA_TAG=$LATEST_TAG" >> $GITHUB_ENV

    - name: 克隆 Mesa 稳定版源码
      run: |
        git clone --depth=1 --branch $MESA_TAG https://gitlab.freedesktop.org/mesa/mesa.git
        cd mesa
        git describe --tags

    - name: 构建 Mesa (Zink only)
      run: |
        cd mesa
        sudo mkdir -p /data/data/com.termux/files/usr/glibc
        sudo chmod 777 -R /data

        mkdir -p builddir
        meson setup builddir \
          --libdir=lib \
          --prefix=/data/data/com.termux/files/usr/glibc \
          -Dbuildtype=release \
          -Dgallium-drivers=zink,freedreno \
          -Dvulkan-drivers= \
          -Dglvnd=disabled \
          -Dplatforms=x11 \
          -Degl-native-platform=x11 \
          -Dxmlconfig=enabled \
          -Dllvm=disabled \
          -Dopengl=true \
          -Dglx=dri \
          -Dglx-direct=true \
          -Dvideo-codecs=all \
          -Dmediafoundation-codecs=all \
          -Degl=enabled

        ninja -C builddir
        ninja -C builddir install

        VERSION=$MESA_TAG
        echo "VERSION=$VERSION" >> $GITHUB_ENV

        cd /data/data/com.termux/files/usr/glibc/lib
        rm -rf dri gbm pkgconfig

        echo "📦 打包库文件..."
        tar -czf $GITHUB_WORKSPACE/termux-glibc-zink-${VERSION}-aarch64.tar.gz *
        du -sh $GITHUB_WORKSPACE/termux-glibc-zink-${VERSION}-aarch64.tar.gz

    - name: 创建或更新 Tag
      run: |
        TAG_NAME="zink-${VERSION}"
        echo "TAG_NAME=$TAG_NAME" >> $GITHUB_ENV

        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

        echo "🔍 检查是否已有 Tag: $TAG_NAME"
        if git ls-remote --tags origin | grep -q "refs/tags/$TAG_NAME"; then
          echo "♻️ 发现同名 Tag，准备更新..."
          git tag -fa "$TAG_NAME" -m "Mesa Zink rebuild $TAG_NAME"
          git push origin ":refs/tags/$TAG_NAME" || true
          git push origin "$TAG_NAME" --force
        else
          echo "🆕 创建新 Tag: $TAG_NAME"
          git tag -a "$TAG_NAME" -m "Mesa Zink auto build for $TAG_NAME"
          git push origin "$TAG_NAME"
        fi

    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: termux-glibc-zink-${{ env.VERSION }}-aarch64
        path: termux-glibc-zink-${{ env.VERSION }}-aarch64.tar.gz

    - name: 发布到 GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ env.TAG_NAME }}
        name: "Zink ${{ env.VERSION }} (aarch64, Termux glibc)"
        draft: false
        prerelease: false
        generate_release_notes: true
        files: termux-glibc-zink-${{ env.VERSION }}-aarch64.tar.gz
        append_body: true
        body: |
          ✅ 自动构建完成！
          - Mesa 版本: ${{ env.VERSION }}
          - 构建平台: Ubuntu 24.04 ARM64
          - Tag: ${{ env.TAG_NAME }}（支持重复版本自动覆盖）
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 输出版本信息
      run: |
        echo "✅ Zink 构建完成"
        echo "版本号: $VERSION"
        echo "Tag: $TAG_NAME"
